package studyEx1;

public class programmers_1 {
    /*public static void main(String[] args) {

        String s= "asdqwe";
        System.out.println(s);
        System.out.println();


    }

    public String solution(String s, String skip, int index) {
        String answer="";
        char[] chararr1=new char[s.length()];
        char[] chararr2=new char[skip.length()];
        char[] temp=new char[s.length()];
        for(int i=0;i<skip.length();i++){
            char[] chararr2=new char[skip.length()];
            chararr2[i]=skip.charAt(i);
        }
        for(int i=0;i<s.length();i++){
            chararr1=new char[s.length()];
            chararr1[i]=s.charAt(i);
        }
        for (int i = 0; i < s.length(); i++) {
            char[] temp=new char[s.length()];
            temp[i]=chararr1[i]+index;
            if (temp[i] >122) {
                temp[i]-=25;
            }
        }





        return answer;
    }*/
}
